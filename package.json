{
  "name": "soulwise",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "tunnel": "sudo service cloudflared start",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.1.30",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "wrangler": "^3.103.2"
  }
}
